const arr = [1, 2, 3, 4];

function myMap(arr) {}

const doubled = myMap(arr);

console.log('arr', arr);
console.log('doubled', doubled);

/*

Функция высшего порядка — это функция, которая может принимать другие функции в качестве аргументов и/или возвращать функцию в качестве результата.
 Такие функции позволяют создавать более абстрактные, гибкие и переиспользуемые решения.

ТЗ:

	1.	Реализовать функцию myMap, которая принимает массив и функцию-обработчик.
	2.	Функция должна возвращать новый массив, где к каждому элементу исходного массива применена переданная функция.
	3.	Оригинальный массив не должен изменяться.
	4.	Функция должна работать с любыми типами данных в массиве.

Пример использования:

function myMap(array, callback) {
  // Твоя реализация
}

const numbers = [1, 2, 3, 4];
const doubled = myMap(numbers, num => num * 2);
console.log(doubled); // [2, 4, 6, 8]
console.log(numbers); // [1, 2, 3, 4] (оригинальный массив не изменен)

*/
